import { PublicKeyBundle, SignedPublicKeyBundle } from './crypto';
export declare function decodeContactBundle(bytes: Uint8Array): PublicKeyBundle | SignedPublicKeyBundle;
export declare function encodeContactBundle(bundle: PublicKeyBundle | SignedPublicKeyBundle): Uint8Array;
