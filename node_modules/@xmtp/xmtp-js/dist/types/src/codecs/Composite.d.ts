import { ContentTypeId, ContentCodec, EncodedContent, CodecRegistry } from '../MessageContent';
export declare const ContentTypeComposite: ContentTypeId;
export declare type Composite = {
    type: ContentTypeId;
    content: any;
} | {
    parts: Composite[];
};
export declare class CompositeCodec implements ContentCodec<Composite> {
    get contentType(): ContentTypeId;
    encode(content: Composite, codecs: CodecRegistry): EncodedContent;
    decode(content: EncodedContent, codecs: CodecRegistry): Composite;
    private toProto;
    private fromProto;
}
